# Regenerated by tauri-plugin's build.rs (tauri_plugin::Builder::android_path)
# at compile time — do not commit.
android/.tauri/
# Gradle build output for the android/ library module — regenerated on build.
android/build/
android/.gradle/
target/
# Auto-generated by tauri_plugin::Builder from the COMMANDS list in build.rs.
permissions/autogenerated/
permissions/schemas/

# cgo build outputs (.so/.h/.a) — rebuilt from source, not tracked
golang/wrapper/*.so
golang/wrapper/*.h
golang/wrapper/*.a

# The mihomo wrapper .so, cross-compiled by build.rs into the Gradle
# module's jniLibs convention directory — regenerated on every build.
android/src/main/jniLibs/
